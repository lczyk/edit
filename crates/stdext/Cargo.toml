[package]
name = "stdext"
version = "0.0.0"

edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
single-threaded = []
# Debug-only invariant checks, defined in `sanity.rs`. Tripped checks write to
# $TMPDIR/edit/log/sanity-YYYYMMDD.log and surface via the notify hook. Turn on
# through a dependent crate's own `sanity` feature rather than directly.
sanity = [
    #
]

[dependencies]
libc = "0.2"
