//! Physics: post-layout, anim-free, full frame IR.
//!
//! See `meanderings/anim_refactor.md` for the target shape. Stage 0
//! placeholder -- types land in stage 1 once `TextBuffer::render` has
//! been split into pure `layout` + `paint`.
