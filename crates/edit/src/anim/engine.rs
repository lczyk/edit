//! Animator: pure perturbation over time.
//!
//! See `meanderings/anim_refactor.md` for the target shape. Stage 0
//! placeholder -- the real `animate(prev, target, t, dt)` lands in
//! stage 4 once `Physics` exists and per-feature state has been
//! displaced off `TextareaContent` / `Tui`.
