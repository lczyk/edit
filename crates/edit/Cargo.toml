[package]
name = "edit"
version = "0.8.2"  # source: /VERSION (synced by `make sync-version`; do not edit by hand)

edition.workspace = true
license.workspace = true
rust-version.workspace = true

build = "build/main.rs"

[dependencies]
eat.workspace = true
gutter.workspace = true
lsh.workspace = true
polyflag.workspace = true
stdext.workspace = true
tty.workspace = true
libc = "0.2"
toml-span = { version = "0.7", default-features = false }
version = { git = "https://github.com/lczyk/version", tag = "rust/v0.5.0" }

[build-dependencies]
stdext.workspace = true
lsh.workspace = true
version = { git = "https://github.com/lczyk/version", tag = "rust/v0.5.0" }
