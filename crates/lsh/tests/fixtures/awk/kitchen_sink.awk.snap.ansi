[32m#!/usr/bin/awk -f[m
[32m# kitchen-sink awk fixture for lsh highlighting[m

[95mBEGIN[m {
    [96mFS[m = [91m","[m
    [96mOFS[m = [91m"\t"[m
    count = [92m0[m
    total = [92m0.0[m
    pi = [92m3.14159[m
    hex = [92m0xFF[m
    sci = [92m1.5e-3[m
    [94mprint[m [91m"starting"[m, [96mFILENAME[m
}

[94mfunction[m [93mfmt[m(n,    s) {
    s = [33msprintf[m([91m"%.2f"[m, n)
    [95mreturn[m s
}

[32m# main loop -- one record per line[m
/^[32m#/ { next }                       # skip comments[m
[96mNF[m >= [92m3[m && [96m$2[m ~ /^[[92m0[m[92m-9[m]+$/ {
    name = [96m$1[m
    qty  = [96m$2[m + [92m0[m
    cost = [96m$3[m + [92m0.0[m
    items[name] += qty
    total += qty * cost
    count++
    [95mif[m (qty > [92m100[m) {
        printf [91m"bulk: %s x%d @%s\n"[m, name, qty, [93mfmt[m(cost)
    } [95melse[m {
        printf [91m"%s\t%d\t%s\n"[m, name, qty, [93mfmt[m(cost)
    }
}

[95mEND[m {
    [95mfor[m (k [95min[m items) {
        [94mprint[m k, items[k]
    }
    printf [91m"total %d records, sum=%s\n"[m, count, [93mfmt[m(total)
    [95mif[m ([96mNR[m == [92m0[m) [95mexit[m [92m1[m
}

