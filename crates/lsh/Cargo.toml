[package]
name = "lsh"
version = "0.0.0"
build = "build.rs"

edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
# Forwards to stdext, where the check macros live.
sanity = [
    "stdext/sanity",
    #
]

[dependencies]
stdext.workspace = true
